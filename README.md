# Flam AI Intern Assignment — The Audit

This assignment audits the previous intern's tokenizer and serving report (`REPORT_v0.md`) before leadership acts on it. The submission lives in `your-submission/`, where `NOTEBOOK.md` logs the work chronologically and `AI_USAGE.md` records where AI helped or misled.

**Part A** (`partA/`) covers the tokenizer audit: a real multilingual eval corpus, measured flaws in the original script and its metric, a corrected cross-tokenizer comparison with denominator reasoning, and a routing and cost memo.

**Part B** (`partB/`) reconciles KV-cache capacity with the benchmark log and explains the reported throughput numbers.

**Part C** (`partC/memo.md`) is the decision memo on making Indic replies sound casual.

The provided `starter_kit/` and `AI ASSIGNMENT 2026.pdf` are inputs to the audit, and `partA/fertility_v0_original.py` preserves the original v0 script for comparison.
